let name = 'Ashwini';

//name = name.toUpperCase();
//name = name.includes("Ash");
// name = name.toLowerCase();
// name = name.split();
// name = name.substring(0,5)
// name = name.substring(2);

// name = name.indexOf('i');

// name = name[1];
name = name.charAt(3);



console.log(name);
